# shared library 
In Jenkins, a Shared Library is a way to reuse common pipeline code across multiple Jenkins jobs 
instead of duplicating the same Groovy scripts in every Jenkinsfile.

#create the folder in GitHub with name name var and create the groovy file
#vars/buildMaven.groovy
def call(String imageName) {
    sh """
       echo "docker build ${imageName}"
    """
}

#Jenkinsfile

Step 1: Install Required Plugin
Pipeline: Shared Groovy Libraries through HTTP retrieval


Step 2: Configure Global Shared Library
Manage Jenkins
 → System
 → Global Trusted Pipeline Libraries


 Click Add.

 Eg:

Name: shared-lib
Default version: master
Allow default version to be overridden: ✓ (optional)
Include @Library changes in job recent changes: ✓ (optional)
Retrieval method: Modern SCM

Step 3: Configure Git Repository
select 
Source Code Management: Git

git@github.com:company/jenkins-shared-library.git

#if its private Repository

Credentials:
github-ssh-key
or
github-token

Eg:
Name: shared-lib
Version: main
SCM: Git
Repository:
git@github.com:company/jenkins-shared-library.git
Credentials:
github-ssh-key

###Jenkinsfile

@Library('shared-lib') _

pipeline {
    agent any

    stages {
        stage('Docker Build') {
            steps {
                buildMaven('myapp')
            }
        }
    }
}



-----------------------------------------------------------
#Email configuration


# Enable 2 step verification
# Add App password


Step 1: Install Email Plugin
Email Extension Template Plugin

Step 2: Configure SMTP Server
Manage Jenkins
 → System
    → Extended E-mail Notification


SMTP Server: smtp.gmail.com
SMTP Port: 587
Use SSL: No
Use TLS: Yes
credential -->Add username and passowrd (gmail and app password)


Step 3: Configure System Email

Manage Jenkins
 → System
    → Jenkins Location

System Admin e-mail address:
jenkins@company.com

Step 4: Test Email

Manage Jenkins
 → System
    → Extended E-mail Notification

Step 5: Use in Jenkins Pipeline

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
    }

    post {
        success {
            emailext(
                subject: "SUCCESS: ${env.JOB_NAME}",
                body: "Build Successful",
                to: "mathanrs14@gmail.com,barathbaby20@gmail.com"
            )
        }
    }
}






